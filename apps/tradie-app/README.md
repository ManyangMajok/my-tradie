# Tradie App

React Native + Expo mobile app for tradespeople.

Do not initialise until Phase 2.A (see `docs/10-build-phases.md`).

When ready:
1. Read `docs/11-mobile-apps-spec.md` in full
2. Initialise with Expo: `pnpm create expo-app tradie-app --template`
3. Follow §2 of the mobile spec for structure
